public enum BallType : byte
{
    // Using actuall numbers, not indexes in array in which can they appear, since the arrays can vary in length between modes.
    Solid = 1,
    Eight = 8,
    Stripe = 9,
    Cue = 16
}